# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
#- task: NodeTool@0
#  inputs:
#    versionSpec: '10.x'
#  displayName: 'Install Node.js'

#- script: |
#    npm install
#    npm run build
#  displayName: 'npm install and build'

- task: Npm@1
  displayName: 'BackendNodejs install'
  inputs:
    command: 'install'
    workingDir: '$(Build.SourcesDirectory)/AppServiceEasyAuth/BackendNodejs'

- task: PublishBuildArtifacts@1
  displayName: 'Publish BackendAPI artifact'
  inputs:
    PathtoPublish: '$(Build.SourcesDirectory)/AppServiceEasyAuth/BackendNodejs'
    ArtifactName: 'backendapi'
    publishLocation: 'Container'